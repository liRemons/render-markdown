# Changesets

To create a new changeset, run:

```bash
npm run changeset
```

Follow the prompts to describe your changes. The changeset will be used to generate changelog and determine the next version during release.